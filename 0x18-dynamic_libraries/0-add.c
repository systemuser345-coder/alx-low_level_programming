#include "calc.h"

/**
 * add - this adds two integers together
 * x: contains the first integer
 * y: contains the second integer
 *
 * Return: returns addition of integers
 */

int add(int x, int y)
{
	return (x + y);
}

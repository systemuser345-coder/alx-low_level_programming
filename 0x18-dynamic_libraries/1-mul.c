#include "calc.h"

/**
 * mul - this multiplies two integers together
 * x: contains the first integer
 * y: contains the second integer
 *
 * Return: returns multiplication of integers
 */

int mul(int x, int y)
{
	return (x * y);
}

#include "calc.h"

/**
 * div - this divides two integers together
 * x: contains the first integer
 * y: contains the second integer
 *
 * Return: returns division of integers
 */

int div(int x, int y)
{
	return (x / y);
}

#include "calc.h"

/**
 * sub - this subtracts two integers together
 * x: contains the first integer
 * y: contains the second integer
 *
 * Return: returns subtraction of integers
 */

int sub(int x, int y)
{
	return (x - y);
}

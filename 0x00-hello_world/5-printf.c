#include <stdio.h>

/**
 * main - Entry point, prints an output using printf function
 *
 * Return: returns 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
